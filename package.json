{
  "name": "4set-arena",
  "version": "1.0.0",
  "description": "Sistema Esportivo 4SET ARENA",
  "private": true,
  "scripts": {
    "build": "cd backend && npm ci && npm run build",
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "workspaces": [
    "backend",
    "frontend"
  ]
}
